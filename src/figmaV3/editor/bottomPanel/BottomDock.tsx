"use client";

import React from "react";

/** 이후 Actions / Data / Console 탭 UI를 담는 도킹 패널 */
const BottomDock: React.FC = () => {
  return (
    <div style={{ borderTop: "1px solid #e5e7eb", padding: 8 }}>
      (BottomDock — Actions / Data / Console 탭 예정)
    </div>
  );
};
export default BottomDock;